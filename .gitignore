node_modules
node_modules/
dist/
*.tsbuildinfo
.astro/
.DS_Store

# v8 coverage output (npm run test:coverage)
coverage/

# Crash dumps — a full-suite run can leave a multi-hundred-MB core file behind
core
core.*

scratch_*

# Generated Jev gate baseline view (regenerable via scripts/jev-gate.sh --html)
jev.baseline.html
jev.baseline.change.html

# Local scratch/working notes — never published
.scratch/
.impeccable/critique/
.superpowers/
.worktrees-local/
.claude/worktrees

.env
harmonic.env
